{
  "name": "gatsby-remark-component",
  "description": "Transform custom component node parent into div",
  "version": "1.0.0",
  "author": "Hebilicious <xsh4k3@gmail.com>",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-runtime": "^6.26.0",
    "unist-util-visit": "^1.1.1"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "gatsby-plugin-remark",
    "component"
  ],
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir . --ignore __tests__",
    "watch": "babel -w src --out-dir . --ignore __tests__",
    "prepublish": "cross-env NODE_ENV=production npm run build"
  },
  "devDependencies": {
    "cross-env": "^5.0.5"
  }
}
