{
  "name": "gatsby-remark-component",
  "description": "When you use a React component in your markdown, it will insert it as a div instead of a p",
  "version": "1.0.0",
  "author": "Hebilicious <xsh4k3@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hebilicious/gatsby-remark-component.git"
  },
  "dependencies": {
    "unist-util-visit": "^1.1.1"
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "gatsby-transformer-remark",
    "component",
    "remark",
    "markdown"
  ],
  "scripts": {
    "test": "jest",
    "build": "babel src --out-file dist/dist.js --ignore __tests__",
    "prepublish": "npm run build && npm run test && minify dist -d dist"
  },
  "license": "MIT",
  "main": "dist/dist.js",
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.39",
    "@babel/core": "^7.0.0-beta.39",
    "@babel/preset-env": "^7.0.0-beta.39",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-jest": "^22.1.0",
    "babel-minify": "^0.3.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^22.1.4",
    "remark": "^9.0.0"
  }
}
